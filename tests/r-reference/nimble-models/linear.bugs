model {
  for (i in 1:N) {
    y[i] ~ dnorm(mu[i], sigma)
    mu[i] <- alpha + beta1 * x1[i] + beta2 * x2[i]
  }
  alpha ~ dnorm(0, 5)
  beta1 ~ dnorm(0, 5)
  beta2 ~ dnorm(0, 5)
  sigma ~ dunif(0, 100)
}
